# Session Handoff

## Type
continuation

## Task Summary
Fix all gaps identified in a full production walkthrough of tenkai-marketing.vercel.app. The product is an AI-powered SEO marketing SaaS ($150-500/mo tiers). A complete Phase 8 customer walkthrough was performed and found 5 issues — 1 P1 and 4 P2. The core problem is a **data pipeline gap**: the queue worker creates entries in the `deliverables` table but doesn't populate feature-specific tables (`audits`, `content_posts`), causing empty states on feature pages.

## Current State
- All email/branding fixes are deployed to production (Resend API key set, FROM email = `onboarding@resend.dev`, support = `rookbot.mini@gmail.com`)
- The app is live at https://tenkai-marketing.vercel.app
- Admin login: `andrewyujisherman@gmail.com` / `Tenkai2026!admin`
- Supabase project: `mvhuxiswjtomvebcgpqr`
- Vercel project ID: `prj_NpVD89lzmJ65y4KgximXtbbR6cGh`

## Key Files
- `/Users/andrewsherman_1/External Clawd/tenkai-marketing/src/app/(portal)/dashboard/page.tsx` — Main dashboard, fetches from `clients`, `content_posts`, `approvals`, `audits`, `deliverables`. Stats show totalContent, pendingApprovals, auditScore, publishedContent. The auditScore comes from `audits` table (not `deliverables`).
- `/Users/andrewsherman_1/External Clawd/tenkai-marketing/src/app/(portal)/health/page.tsx` — SEO health page, queries `audits` table
- `/Users/andrewsherman_1/External Clawd/tenkai-marketing/src/app/(portal)/content/page.tsx` — Content page, queries `content_posts` table
- `/Users/andrewsherman_1/External Clawd/tenkai-marketing/src/app/(portal)/reports/ReportsClient.tsx` — Reports page, queries `deliverables` table but shows empty
- `/Users/andrewsherman_1/External Clawd/tenkai-marketing/src/app/(portal)/integrations/page.tsx` — Integrations page, left panel appears empty
- `/Users/andrewsherman_1/External Clawd/tenkai-marketing/src/lib/email.ts` — Email utility (already fixed)
- `/Users/andrewsherman_1/External Clawd/tenkai-marketing/.env.local` — Environment variables
- `/Users/andrewsherman_1/External Clawd/tenkai-marketing/src/middleware.ts` — Auth middleware

## Decisions Made
- Use `rookbot.mini@gmail.com` for all support/contact emails (NOT tenkai.marketing)
- Use `onboarding@resend.dev` as FROM email (Resend shared domain)
- Resend API key: `re_MAtGAxuS_5v48c74YJUEXpt3DGBpJmNh6` (already set in Vercel env)
- The 9 AI agents are Japanese-themed: Haruki, Kenji, Sakura, Rin, Yuto, Aiko, Hana, Mei, Daichi

## Issues to Fix (Priority Order)

### P1: Dashboard audit score inconsistency
- **What:** Dashboard stat card shows "no audit yet" but `deliverables` table has an "SEO Site Audit" entry with score 25/100
- **Root cause:** Dashboard queries the `audits` table for audit score, but the worker only writes to `deliverables` — never populates `audits`
- **Fix approach:** Make dashboard/health pages fall back to reading from `deliverables` when `audits` is empty. OR better: when a deliverable of type "audit" is created, also insert/update the `audits` table. The simpler fix is to update the dashboard queries.

### P2: /health page empty state
- **What:** Shows "Kenji will run your first SEO audit after onboarding" despite user being onboarded and audit deliverable existing
- **Root cause:** Same as P1 — health page reads from `audits` table which is empty
- **Fix:** Same approach as P1

### P2: /content page empty state
- **What:** Shows "No topics yet" despite content data potentially existing
- **Root cause:** Needs investigation — check if `content_posts` has data for admin's client_id, or if the worker doesn't populate this table
- **Fix:** Query Supabase first to understand data state, then fix queries

### P2: /integrations left panel empty
- **What:** The gray area on the left of the integrations page has no content rendered
- **Root cause:** Needs investigation — read the integrations page component
- **Fix:** Read the component, understand what should render there, fix it

### P2: /reports empty state
- **What:** All report tabs show "No reports yet" despite deliverable data existing
- **Root cause:** Reports page may be querying wrong table or filtering incorrectly
- **Fix:** Read ReportsClient.tsx, trace the query, fix filtering

## Next Steps
1. **Query Supabase** to understand actual data state — what's in `deliverables`, `audits`, `content_posts`, `approvals` for the admin client (andrewyujisherman@gmail.com)
2. **Fix P1 first** — Dashboard audit score. Make it read from `deliverables` as fallback
3. **Fix P2: /health** — Same data source fix
4. **Fix P2: /content** — Investigate and fix content_posts query
5. **Fix P2: /integrations** — Investigate and fix left panel
6. **Fix P2: /reports** — Investigate and fix report queries
7. **Build and deploy** — `npx vercel --prod` from the project directory
8. **Re-verify** all pages on production

## Context for New Session
- The Vercel CLI is authenticated (no token file needed, just use `npx vercel` directly)
- Supabase service role key is in `.env.local` line 4
- Use `SUPABASE_SERVICE_ROLE_KEY` to query Supabase directly via REST API or the JS client
- The project uses Next.js App Router with `src/app/` directory structure
- Portal pages are under `src/app/(portal)/` with layout providing sidebar nav
- All portal pages are server components that query Supabase server-side
- The worker code lives in `src/lib/agents/` and `src/app/api/` routes
- After fixing code, deploy with: `cd "/Users/andrewsherman_1/External Clawd/tenkai-marketing" && npx vercel --prod`

## Original User Request
"a lot of it is built out but there are a lot of gaps. i need you to see everything we have, and start the process over and make this customer ready."
Then after seeing the audit: "/spawn a new session to fix everything"
